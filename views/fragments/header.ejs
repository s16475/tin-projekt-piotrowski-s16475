<!DOCTYPE html>
<html lang="pl-PL">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AXE TUiR S.A.</title>
    <link rel="stylesheet" href="/css/style.css" />
    <script type="application/javascript" src="/js/validationCommon.js"></script>
    <script type="application/javascript" src="/js/validationClaimForm.js"></script>  
    <script type="application/javascript" src="/js/validationEmpForm.js"></script>
</head>
<body>
<header>
    <h1>AXE TUiR S.A.</h1>
    <img src="/images/logo.png" width="100" height="100" alt="AXE TUiR S.A. Logo">
</header>

<% if(!loggedUser) { %>
    <div id="login">
        <h5><%= __('nav.login') %></h5>
        <form method="post" action="/login" novalidate>
            <label for="loginEmail"><%= __('nav.email') %></label>
            <input type="email" id="loginEmail" name="email">

            <label for="loginPassword"><%= __('nav.pass') %></label>
            <input type="password" id="loginPassword" name="password">

            <input type="submit" value="<%= __('nav.login2') %>">
            <span id="loginErrors"><%= loginError ? loginError : ''%></span>
        </form>
    </div>
<% } else { %>
    <span><%= __('nav.user') %> <%=loggedUser.firstName + " " + loggedUser.lastName%></span>
    <a href="/logout"><%= __('nav.logout') %></a>
<% } %>

<nav>
    <ul>
        <% if(loggedUser) { %>
        <li><a href="/" class="<%= navLocation == 'main' ? 'active' : ''%>"><%= __('nav.main-page') %></a></li>
        <li><a href="/employees" class="<%= navLocation == 'emp' ? 'active' : ''%>"><%= __('nav.employees') %></a></li>
        <li><a href="/claims" class="<%= navLocation == 'claim' ? 'active' : ''%>"><%= __('nav.claims') %></a></li>
        <li><a href="/policies" class="<%= navLocation == 'policy' ? 'active' : ''%>"><%= __('nav.policies') %></a></li>
        <li><a href="/changeLang/pl" class="<%= lang == 'pl' ? 'active' : ''%>">PL</a></li>
        <li><a href="/changeLang/en" class="<%= lang == 'en' ? 'active' : ''%>">EN</a></li>
        <% } %>
    </ul>
</nav>